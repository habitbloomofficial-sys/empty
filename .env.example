# --- Required: JARVIS's brain — set ONE of these two providers ---

# Option A: OpenAI
OPENAI_API_KEY=
OPENAI_MODEL=gpt-4o

# Option B: Gemini (get a free key at https://aistudio.google.com/apikey)
GEMINI_API_KEY=
GEMINI_MODEL=gemini-2.5-flash

# If both keys above are set, OpenAI is used by default. Force one explicitly
# with AI_PROVIDER=openai or AI_PROVIDER=gemini.
AI_PROVIDER=

# --- Required for voice replies ---
ELEVENLABS_API_KEY=
# Optional: pick a specific ElevenLabs voice id (defaults to "Adam")
ELEVENLABS_VOICE_ID=
ELEVENLABS_MODEL_ID=eleven_turbo_v2_5

# --- Optional: Gmail integration (see README for setup) ---
GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=
GOOGLE_REDIRECT_URI=http://localhost:3000/api/gmail/callback

# --- Optional: WhatsApp integration via Twilio (see README for setup) ---
TWILIO_ACCOUNT_SID=
TWILIO_AUTH_TOKEN=
TWILIO_WHATSAPP_FROM=whatsapp:+14155238886
TWILIO_WHATSAPP_TO_DEFAULT=
